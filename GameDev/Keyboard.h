#ifndef KEYBOARD_H_
#define KEYBOARD_H_
#include "Globals.h"

class Keyboard {

public:
	Keyboard();
	virtual ~Keyboard();
	//void setPlayer(Player player);
	//Player getPlayer();
	//void setMenu(Menu menu);
	//Menu getMenu();
	void setRunning();


private:

};

#endif /* KEYBOARD_H_ */
